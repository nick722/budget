import React from "react";
import ReactModal from "react-modal";

const AddTransactionModal = ({ isOpen }) => {
  return <ReactModal isOpen={isOpen}>AddTransactionModal</ReactModal>;
};

export default AddTransactionModal;
